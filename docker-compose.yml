version: "3.0"

services:
  backend:
    build: ./backend
    container_name: backend
    command:
      sh -c "uvicorn app.main:app --reload --workers 1 --host 0.0.0.0 --port 8000"
    volumes:
      - ./backend:/usr/src/app
    ports:
      - "8000:8000"